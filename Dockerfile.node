FROM node:18-alpine

WORKDIR /app

# Copy package files
COPY package.json ./
COPY webpack.config.js ./

# Install dependencies
RUN npm install

# Create src directory
RUN mkdir -p src dist

EXPOSE 3000

CMD ["npm", "run", "dev"]